# YoTouch AI Service

FastAPI microservice for biometric embeddings, liveness scores, and fraud heuristics.

## Quick start

```
python -m venv .venv
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
pip install -e .
uvicorn app.main:app --reload --port 9000
```
